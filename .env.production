# Production Environment Variables for cPanel Deployment

# API Configuration - Point to your Vercel backend
REACT_APP_API_BASE_URL=https://construction-website-black.vercel.app

# Note: These variables are embedded into the build at compile time
# They will be publicly visible in the browser, so don't put sensitive data here

